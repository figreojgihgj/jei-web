# 示例题：3x3 L 形入门

这是一个最小可运行的收录示例，用于验证：

- `index.json` 能列出题目
- `json` 能被解析为关卡
- `markdown` 能在收录页展示
- 可以一键在试玩页或编辑器打开

## 关卡说明

- 棋盘大小：`3 x 3`
- 提示格：左上角 `L` 形三格
- 方块：只有一个 `L` 形方块，数量 `1`

## 你可以怎么扩展

1. 在同目录新增 `your-level.json` 和 `your-level.md`
2. 在 `index.json` 的 `entries` 中新增一条
3. 刷新“题目收录”页面即可看到新题
